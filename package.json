{
  "name": "tictactoe-vuejs",
  "version": "1.0.0",
  "description": "A basic multiplayer tic tac toe, using sockets",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client/ && yarn install && yarn build"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ],
  "dependencies": {
    "express": "^4.17.2",
    "socket.io": "^2.4.1"
  }
}
